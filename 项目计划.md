# 企业级 RAG 知识库问答系统 — 实施计划

> 目标：把《项目架构与优化说明书.md》从设计蓝图落地为**可运行、可讲解、可拿去面试**的完整项目。
> 交付形态：**Docker 全栈**（FastAPI + Celery + MySQL + Redis + Streamlit 前端）。
> 模型：**通义千问（DashScope）** — LLM + Embedding + Reranker 全部走 Qwen API。

---

## 0. 一句话概括

构建一个企业级 RAG 知识库问答系统：上传 PDF → 异步结构化解析 → 语义切分（父子文档策略）→ 混合检索（FAISS + BM25）→ 重排序 → 流式生成回答并附引用来源。全程可观测、可部署、有测试。

---

## 1. 技术栈与版本（对齐说明书）

| 领域 | 选型 | 说明 |
| :--- | :--- | :--- |
| 后端 | FastAPI + Uvicorn | 异步，自动 OpenAPI |
| 异步任务 | Celery + Redis | 文档解析/向量化异步化，避免上传超时 |
| 数据库 | MySQL 8 + SQLAlchemy 2.0（asyncmy） | 用户/文档/会话/历史 |
| 向量库 | FAISS（本地，持久化） | 语义检索 |
| 文档解析 | PyMuPDF (fitz) | 保留标题层级，表格转 Markdown |
| 切分 | LangChain `MarkdownHeaderTextSplitter` | 按语义块切分 + 父子文档 |
| 混合检索 | FAISS + BM25（rank_bm25） | 双路召回取并集 |
| 重排 | Qwen Rerank（gte-rerank） | 二次精排，Top-50 → Top-5 |
| 大模型 | Qwen（qwen-plus / qwen-turbo） | 流式生成 |
| 可观测 | LangSmith（可选，按 Key 开关） | 全链路 Trace |
| 前端 | Streamlit | 页面简单、容器内一条命令跑 |
| 部署 | Docker Compose | 一键拉起 API + Worker + MySQL + Redis + Web |

---

## 2. 目录结构（规划）

```
E:\实习\AI系统\
├── 项目架构与优化说明书.md
├── 项目计划.md                  ← 本文件
├── README.md                    ← 项目说明 + 启动/演示/面试讲解
├── .env.example                 ← 环境变量模板（含 API Key 占位）
├── docker-compose.yml           ← 一键启动
├── backend/
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── app/
│   │   ├── main.py              # FastAPI 入口
│   │   ├── config.py            # pydantic-settings 配置
│   │   ├── database.py          # 异步引擎/会话
│   │   ├── models/              # users / documents / chat_sessions / chat_history
│   │   ├── schemas/             # Pydantic 请求/响应模型
│   │   ├── api/                 # auth / documents / chat 路由
│   │   ├── core/                # JWT、密码哈希、全局异常、统一响应
│   │   ├── services/
│   │   │   ├── rag/             # parser / splitter / vectorstore / retriever / reranker / llm / pipeline
│   │   │   ├── chat_service.py
│   │   │   └── document_service.py
│   │   └── tasks/               # celery_app.py + document_tasks.py
│   ├── tests/                   # pytest：parser / splitter / retriever / auth
│   └── scripts/                 # init_db.py / seed.py
├── frontend/
│   ├── Dockerfile
│   └── app.py                   # Streamlit 界面
└── data/                        # FAISS 索引 + 上传文件（挂载卷，gitignore）
```

---

## 3. 关键设计决策（面试会问，提前想清楚）

1. **异步工程化**：上传接口立即返回 `task_id`；`documents.status` 状态机 `PENDING→PROCESSING→COMPLETED/FAILED`；前端轮询进度。
2. **父子文档 (Parent-Child Retriever)**：大块 Parent 切小块 Child 存向量；检索命中 Child，取回其 Parent 喂给大模型 —— 兼顾召回精准度与上下文完整度。
3. **混合检索**：向量（语义）+ BM25（专有名词/关键词）双路取并集，再 Rerank 精排，解决"专有名词失效"与"幻觉"。
4. **模型工厂设计**：`services/rag` 内抽象 LLM/Embedding/Reranker 接口，默认 DashScope，可切换到本地模型（Ollama/bge）—— 体现可扩展架构。
5. **LangSmith 可选开关**：无 Key 也能跑，有 Key 自动开启全链路 Trace，方便面试演示 Bad Case 排查。
6. **统一响应 + 全局异常**：封装 `{code, message, data}`，保障健壮性。

---

## 4. 实施步骤（分阶段，每阶段可验收）

| 阶段 | 内容 | 产出/验收 |
| :--- | :--- | :--- |
| **P1 骨架** | 后端目录结构、config、database、models、统一响应/异常、JWT 认证 | `POST /api/auth/register`、`/login` 可用 |
| **P2 文档链路** | 文档上传 + Celery 任务 + 状态机 + 解析器(fitz) + 切分器(父子) | 上传 PDF 后状态流转到 COMPLETED |
| **P3 检索生成** | FAISS 向量库(持久化) + BM25 混合检索 + Reranker + Qwen 流式 RAG 流水线 | 有向量库后问答可流式输出并带引用 |
| **P4 会话与引用** | chat_sessions / chat_history 持久化、历史会话列表、引用来源 JSON | 前端可切换历史会话、点击引用 |
| **P5 前端** | Streamlit：登录→上传→问答流式→引用高亮→历史列表 | 全流程可交互演示 |
| **P6 工程化** | Dockerfile + docker-compose + .env.example + pytest 测试 + README | 一键 `docker compose up` 跑通全集 |
| **P7 打磨** | 测试样例 PDF、seed 数据、面试讲解文档、压测/优化说明 | 可拿去面试自评 |

---

## 5. 环境配置（你需要在 Windows 机器上准备的）

> 以下在**你本机**（运行 Docker 的机器）执行，沙箱里只负责写代码。

### 5.1 前置软件
- **Python 3.10+**（本地开发/跑测试用，部署可全靠 Docker）
- **Docker Desktop（Windows）** — 必须
  - 安装后：Settings → General → 勾选 **Use WSL 2 based engine**；Resources 给足内存（建议 ≥ 4GB）
  - 验证：`docker version`、`docker compose version` 均能输出
- **（可选）Git** — 便于版本管理

### 5.2 通义千问 API Key
- 访问 DashScope 控制台（阿里云百炼）开通：https://bailian.console.aliyun.com/
- 创建 API-KEY，权限开通：`qwen-plus`（对话）、`text-embedding-v3`（向量）、`gte-rerank`（重排）
- 把 Key 填入 `.env` 的 `DASHSCOPE_API_KEY`

### 5.3 我的需求说明
- 你在沙箱里看不到本机 Docker，我会**直接写代码 + 提供启动脚本**，代码正确性通过沙箱内 `pytest` 验证。
- 最终由你本机 `docker compose up -d` 跑起来。若遇到问题，随时把报错贴给我。

---

## 6. 风险与对策

| 风险 | 对策 |
| :--- | :--- |
| 无 LangSmith Key，观测链路断了 | 做成可选开关，无 Key 自动跳过，不影响主流程 |
| FAISS 本地索引多进程不一致 | 单 API + 单 Worker 部署，索引持久化到共享卷 |
| 本地 rerank 模型下载慢 | 默认走 DashScope `gte-rerank` API，不下载本地模型 |
| 首次启动拉镜像慢 | 国内可配 Docker 镜像加速；依赖锁定版本 |
| 面试问"这部分怎么实现的" | 每模块写清晰注释 + README 面试讲解章节 |

---

## 7. 下一步

确认本计划后，我从 **P1 骨架**开始动手，按阶段推进并在每阶段汇报进度。你可以随时调整优先级或补充要求。