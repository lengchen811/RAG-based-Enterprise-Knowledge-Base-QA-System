# 面试讲解与项目介绍

> 这份文档帮你把项目讲清楚、讲得精彩，拿去面试 AI 应用开发 / RAG 方向岗位。
> 配合 README 与 `项目架构与优化说明书.md` 使用。

---

## 一、30 秒电梯介绍（开场白）

> "我独立实现了一个**企业级 RAG 知识库问答系统**。用户上传 PDF 或 Markdown 文档，系统自动解析、向量化入库，然后就能基于文档内容进行多轮流式问答，并附可点击的引用来源。相比普通 Demo，我在四个点上做了生产级优化：**复杂文档结构化解析、父子文档切分、混合检索加重排序、异步工程化与全链路可观测**。技术栈是 FastAPI + Celery + MySQL + FAISS + 通义千问，用 Docker Compose 一键部署。"

---

## 二、项目亮点（按面试官喜好排序）

### 亮点 1：异步工程化 —— 体现「生产级架构」思维
- 上传百页 PDF 同步处理会撑爆 HTTP 超时，所以用 **Celery + Redis** 异步化。
- 接口**秒回 task_id**，数据库状态机 `PENDING → PROCESSING → COMPLETED/FAILED` 追踪进度。
- 前端轮询状态，失败可重试。
- **面试加分点**：会说"为什么用异步而不是多线程"——因为解析/向量化是 IO 密集 + 计算密集，Celery 能横跨进程、支持重试、解耦 API 与 Worker，扩展性强。

### 亮点 2：混合检索 + 重排序 —— 体现「解决真实问题」能力
- 纯向量检索对**专有名词**（工号、产品型号）失效 → 加 **BM25** 关键词召回。
- 双路召回后，用 **Cross-Encoder（gte-rerank）** 对 Top-50 精排到 Top-5。
- **面试加分点**：能讲清"为什么双塔不够、要 Cross-Encoder"——双塔是粗排、Cross-Encoder 是精排，精度更高但慢，所以先用向量粗召回再精排，是工业界标准做法。

### 亮点 3：父子文档（Parent-Child Retriever）—— 体现「对 RAG 深入理解」
- 固定长度切分破坏段落语义 → 用 `MarkdownHeaderTextSplitter` 按标题切成语义完整的 Parent。
- Parent 再切 Child 用于向量检索，命中 Child 后**还原 Parent 上下文**喂给大模型。
- **面试加分点**：能解释"检索要精准（小块）、生成要完整（大块）"的矛盾统一。

### 亮点 4：复杂文档解析 —— 体现「工程细节」能力
- 用 PyMuPDF 而非传统按页提取，保留标题层级、表格转 Markdown。

### 亮点 5：全链路可观测 —— 体现「线上运维」意识
- 接入 LangSmith，记录检索召回、排序分数、Prompt 拼装、Token 消耗。
- 能回答"排查 Bad Case 的流程"。

---

## 三、高频面试问题（附答题思路）

### Q1：什么是 RAG？为什么需要它？
**答**：RAG = 检索增强生成。大模型训练数据有截止时间、且不了解私有知识，直接问会"瞎编"。RAG 先从知识库检索相关文档，再把这些内容作为上下文"喂"给大模型，让它基于资料作答。好处：知识可更新（不用重训）、答案可溯源、降低幻觉。

### Q2：向量检索的工作原理？
**答**：把文本用 Embedding 模型转成高维向量，语义相近的文本向量距离近。检索时把查询也转成向量，用余弦相似度找最接近的 Top-K。本项目用 FAISS 做索引，归一化后用内积等价于余弦相似度。

### Q3：为什么向量检索对专有名词失效？
**答**：Embedding 是"语义"匹配，专有名词（如工号 A1001）在语义空间里没有分布，容易被忽略。而 BM25 是字面匹配，能精确命中。所以两者互补。

### Q4：怎么降低幻觉？
**答**：三个层面——① 检索质量（混合检索 + 重排序保证召回相关）；② Prompt 约束（系统提示"只基于资料回答，没有就说明"）；③ 可观测（LangSmith 追踪，定位是检索还是生成的问题）。

### Q5：为什么用异步任务而不是同步？
**答**：见亮点 1。关键字：不阻塞 HTTP、跨进程、可重试、可扩展。

### Q6：FAISS 和向量数据库（Milvus）的区别？
**答**：FAISS 是本地内存库，单机、无法跨进程共享索引，适合中小规模；Milvus/Qdrant 是分布式向量数据库，支持多副本、标量过滤、水平扩展，适合海量数据。本项目单机用 FAISS，升级方向是 Milvus。

### Q7：状态机为什么这么设计？
**答**：让文档处理可追踪、可重试、可对账。PENDING 表示已接收未处理，PROCESSING 表示进行中，COMPLETED/FAILED 是终态。Worker 崩溃时通过状态恢复。

### Q8：流式输出怎么实现的？
**答**：后端用 SSE（Server-Sent Events），把大模型一次 token 一次 token 地推给前端，前端逐字渲染。比一次性返回体验好，首字延迟低。

### Q9：如果回答质量差，你怎么排查？
**答**：用 LangSmith 看 Trace——先看检索是否召回了正确内容（如果没召回，问题在切分/检索/Embedding）；再看排序分数（如果召回对但排在后，问题在 Reranker）；最后看 Prompt 和模型输出。分层定位。

### Q10：项目里最难的 bug 是什么？
**答**：可以讲 async SQLAlchemy 的 lazy-load 问题（relationship 懒加载在异步会抛 MissingGreenlet），改成显式查询解决；以及 Celery worker 与 API 共享 FAISS 索引的一致性，用单 Worker + 共享卷解决。

---

## 四、简历建议（可放入项目经历）

> **企业级 RAG 知识库问答系统** · 独立开发 · 2026
> - 基于 FastAPI + SQLAlchemy(异步) + MySQL + Redis 构建后端，Celery 异步处理文档解析与向量化，支持上传百页 PDF 不超时。
> - 实现 PyMuPDF 结构化解析、父子文档切分、FAISS 向量 + BM25 混合检索、Cross-Encoder 重排序，显著降低幻觉。
> - 支持多轮流式问答（SSE）与引用溯源；接入 LangSmith 全链路追踪；20 个 pytest 单元测试全绿；Docker Compose 一键部署。
> - 技术栈：Python, FastAPI, Celery, MySQL, FAISS, LangChain, 通义千问, Docker, Streamlit

---

## 五、演示脚本（面试现场 Demo 流程）

1. 打开 http://localhost:8501，注册/登录（admin / admin123）。
2. 上传 `data/seeds/员工手册示例.pdf`，等待状态变 ✅。
3. 提问："员工年假有几天？" —— 展示流式输出 + 引用来源。
4. 上传 `data/seeds/信息安全管理制度.pdf`，提问"密码策略有哪些要求？" —— 展示**多文档检索**。
5. 提问一个知识库外的问题（如"公司食堂在哪？"）—— 展示"资料中未找到"的拒答，说明降幻觉。
6. 切换左侧历史会话 —— 展示多轮对话与持久化。
7. 注册第二个账号 `user2` 登录，展示**看不到** admin 的文档 —— 权限隔离。
8. 打开 http://localhost:8000/docs —— 展示 Swagger API 文档。
9. （加分）打开 LangSmith 看一次请求的完整 Trace。

---

## 五点五、已验证能力清单（面试可直接引用）

> 以下每一项都经过真实运行验证，可作为简历/面试的"可验证"背书。

| 能力 | 验证方式 | 结果 |
| :--- | :--- | :--- |
| 文档处理链路 | 上传 PDF → 状态机 COMPLETED | ✅ 自动解析、切分、向量化 |
| 语义检索 | 问"年假"命中员工手册 | ✅ 命中正确章节 |
| 关键词召回 | 问"报销"命中报销章节 | ✅ BM25 补充召回 |
| 拒答（降幻觉） | 问知识库外问题 | ✅ 明确"未找到"不编造 |
| 多轮对话 | 追问"事假怎么申请" | ✅ 结合上下文 |
| 多文档聚合 | 问"密码策略"命中信息安全制度 | ✅ 跨文档检索正常 |
| 多用户隔离 | user2 看不到 admin 文档 | ✅ 按 user_id 隔离 |
| 数据持久化 | docker compose down 后重启 | ✅ 数据不丢 |
| 单元测试 | pytest | ✅ 20/20 通过 |
| 流式问答 | 逐字输出 | ✅ SSE 正常 |

### 面试讲这个项目的"三个递进亮点"

1. **工程化**：上传秒回、异步处理、状态机 —— "不是跑通的 Demo，是按照生产标准做的"。
2. **检索质量**：复杂文档解析 + 父子切分 + 混合检索 + 重排序 —— "懂 RAG 核心技术，且解决过真实问题"。
3. **多租户与可靠性**：多用户隔离 + 数据持久化 + 可观测 —— "有企业级架构意识"。

---

## 六、进阶问题（如果被追问）

- **为什么选 text-embedding-v3 / gte-rerank？** 通义系模型对中文效果好，统一走 DashScope 一个 Key，架构简单。
- **如何做多租户/权限隔离？** 目前按 user_id 隔离文档与会话；向量检索按 document_id + user_id 过滤（升级方向）。
- **如何扩展到大文档/多文档？** 目前支持多文档聚合；可加文档级过滤、Metadata 过滤。
- **如何评测 RAG 效果？** 可用 RAGAS（faithfulness / context_recall / answer_relevancy）在线评测。
- **安全？** JWT 鉴权、密码 bcrypt 哈希、上传大小与格式校验、路径穿越防护（唯一文件名）。