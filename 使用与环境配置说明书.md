# 企业级 RAG 知识库问答系统 — 使用与环境配置说明书

> 本文档面向**首次使用者**，从零开始：装环境 → 配 Key → 启动 → 使用 → 排错。
> 配套：`README.md`（项目总览）、`教学面试讲解与自我介绍.md`（面试准备）。

---

## 目录

1. [环境准备（前置软件）](#一环境准备前置软件)
2. [申请通义千问 API Key](#二申请通义千问-api-key)
3. [环境配置（.env 详解）](#三环境配置env-详解)
4. [一键启动（Docker 方式）](#四一键启动docker-方式)
5. [本地开发模式（不依赖 Docker）](#五本地开发模式不依赖-docker)
6. [系统使用说明](#六系统使用说明)
7. [API 接口说明](#七api-接口说明)
8. [运行测试](#八运行测试)
9. [常见问题排查（FAQ）](#九常见问题排查faq)

---

## 一、环境准备（前置软件）

本项目推荐用 **Docker Desktop** 一键部署，无需手动装数据库。

### 1.1 安装 Docker Desktop（Windows）

1. 下载并安装 [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop/)。
2. 安装完成后，打开 Docker Desktop：
   - **Settings → General** → 勾选 **Use the WSL 2 based engine**（WSL2 引擎，性能更好）。
   - **Settings → Resources** → 把 **Memory** 调到 **≥ 4GB**，CPU 建议 4 核。
3. 等待 Docker Desktop 右下角图标变绿（显示 "Engine running"）。
4. 验证安装：打开 PowerShell 或 CMD，运行：

```powershell
docker --version
docker compose version
```

能正常输出版本号即安装成功。

> 若提示 `docker: command not found`，说明没装好或 PATH 未生效，重启电脑或重装。

### 1.2 安装 Git（可选，推荐）

用于版本管理，方便面试时展示 commit 记录。

```powershell
git --version
```

### 1.3 安装 Python（仅本地开发模式需要）

Docker 部署**不需要**本机 Python。若想用本地开发模式（见第 5 节），需装 Python 3.10+。

---

## 二、申请通义千问 API Key

本项目的大模型、向量化、重排序都走**通义千问（DashScope，阿里云百炼）**。

### 2.1 注册与开通

1. 打开阿里云百炼控制台：https://bailian.console.aliyun.com/
2. 用阿里云账号登录（没有则注册；新用户通常有免费额度）。
3. 进入 **API-KEY 管理**，点击 **创建 API-KEY**，复制生成的 `sk-...` 字符串。

### 2.2 确认模型权限

确保你的账号已开通以下三个模型（免费额度通常覆盖）：

| 用途 | 模型 | 说明 |
| :--- | :--- | :--- |
| 对话生成 | `qwen-plus` | 生成回答 |
| 文本向量化 | `text-embedding-v3` | 把文档/问题转成向量 |
| 重排序 | `gte-rerank` | 对召回结果精排 |

> 若调用时报权限错误，到百炼控制台的「模型广场」搜索并开通对应模型。

---

## 三、环境配置（.env 详解）

### 3.1 创建 .env 文件

打开项目根目录（`E:\实习\AI系统`），复制模板：

```powershell
# 在项目根目录执行
copy .env.example .env
```

### 3.2 逐项填写

打开 `.env`，重点填这两个：

```ini
# ===== 必填：通义千问 API Key =====
DASHSCOPE_API_KEY=sk-你申请到的key

# ===== 认证密钥（建议改成随机长字符串）=====
JWT_SECRET=改成一段随机长字符串
```

其余可保持默认。`.env` 完整说明：

| 变量 | 默认值 | 说明 |
| :--- | :--- | :--- |
| `DASHSCOPE_API_KEY` | 空 | **必填**，通义千问密钥 |
| `DATABASE_URL` | `mysql+asyncmy://rag:rag2024@mysql:3306/ragdb` | Docker 内置 MySQL，无需改 |
| `REDIS_URL` | `redis://redis:6379/0` | Docker 内置 Redis，无需改 |
| `JWT_SECRET` | 占位 | **改掉**，用于登录令牌加密 |
| `LANGCHAIN_TRACING_V2` | `false` | 是否开 LangSmith 追踪 |
| `LANGCHAIN_API_KEY` | 空 | 有 LangSmith 才填 |
| `LLM_MODEL` | `qwen-plus` | 可选，换模型 |
| `EMBEDDING_MODEL` | `text-embedding-v3` | 可选 |
| `RERANK_MODEL` | `gte-rerank` | 可选 |

> ⚠️ `.env` 含密钥，**已在 `.gitignore` 中排除**，不要提交到 git。

---

## 四、一键启动（Docker 方式）

### 4.1 启动整套集群

在项目根目录（含 `docker-compose.yml` 的目录）打开 PowerShell 或终端：

```powershell
# 首次会拉取镜像 + 构建，较慢，耐心等待
docker compose up -d --build
```

### 4.2 查看状态

```powershell
# 查看所有容器状态
docker compose ps

# 实时查看日志（Ctrl+C 退出）
docker compose logs -f
```

出现 5 个服务且状态为 `Up` 即成功：

```
NAME        STATUS
rag-mysql   Up
rag-redis   Up
rag-api     Up
rag-worker  Up
rag-web     Up
```

### 4.3 访问系统

| 入口 | 地址 |
| :--- | :--- |
| **前端界面** | http://localhost:8501 |
| **API 文档（Swagger）** | http://localhost:8000/docs |
| API 健康检查 | http://localhost:8000/health |

### 4.4 停止 / 重启

```powershell
docker compose down          # 停止并删除容器（数据仍在卷里）
docker compose stop          # 只停止
docker compose restart       # 重启
docker compose up -d         # 重新启动
```

### 4.5 彻底重置（清空数据）

```powershell
docker compose down -v       # -v 会删除数据卷，慎用
```

---

## 五、本地开发模式（不依赖 Docker）

> 适合调试代码。需要本机已装 Python 3.10+、MySQL、Redis。

### 5.1 准备数据库

建议用 Docker 只起 MySQL 和 Redis：

```powershell
docker run -d --name rag-mysql -p 3306:3306 -e MYSQL_ROOT_PASSWORD=root -e MYSQL_DATABASE=ragdb -e MYSQL_USER=rag -e MYSQL_PASSWORD=rag2024 mysql:8.0
docker run -d --name rag-redis -p 6379:6379 redis:7-alpine
```

并把 `.env` 里的地址改成 `localhost`（去掉 compose 内部服务名）：

```ini
DATABASE_URL=mysql+asyncmy://rag:rag2024@localhost:3306/ragdb
REDIS_URL=redis://localhost:6379/0
```

### 5.2 初始化后端

```powershell
cd backend
python -m venv .venv
.venv\Scripts\activate          # Windows
pip install -r requirements.txt
```

### 5.3 启动三个进程

```powershell
# 终端 1：API
uvicorn app.main:app --reload --port 8000

# 终端 2：Worker（解析文档用）
celery -A app.tasks.celery_app:celery_app worker --loglevel=info

# 终端 3：前端
cd ..\frontend
pip install -r requirements.txt
streamlit run app.py
```

---

## 六、系统使用说明

### 6.1 注册 / 登录

打开 http://localhost:8501，进入「注册」页签创建账号，或用「登录」页签登录。

> 演示账号：`admin` / `admin123`（首次进入需先注册）。

### 6.2 上传文档

1. 在**左侧边栏**「📄 上传文档」区域，点击选择文件；
2. 支持 `.pdf` / `.md` / `.txt`，单文件 ≤ 20MB；
3. 点击「上传并解析」，右侧显示「已上传，后台解析中…」；
4. 在「🗂 已上传文档」列表观察状态：`⏳ PENDING/PROCESSING → ✅ COMPLETED`。

> 上传是异步的，处理完成前先别提问该文档的内容。

### 6.3 发起问答

在底部输入框输入问题，回车：

- 回答会**逐字流式**输出（打字机效果）；
- 回答下方会显示「📎 引用来源」，可展开查看具体出自哪个文档、哪一段。

### 6.4 多轮对话

同一个会话内连续提问，系统会记住上下文（保留最近 6 条）。

### 6.5 历史会话管理

- 左侧「💬 历史会话」下拉框选择会话 → 点「加载该会话」恢复；
- 「➕ 新对话」开启新会话；
- 「🚪 退出登录」退出。

---

## 七、API 接口说明

打开 http://localhost:8000/docs 可在线调试。核心接口：

| 方法 | 路径 | 说明 |
| :--- | :--- | :--- |
| POST | `/api/auth/register` | 注册 |
| POST | `/api/auth/login` | 登录，返回 token |
| GET | `/api/auth/me` | 当前用户 |
| POST | `/api/documents/upload` | 上传文档（异步处理） |
| GET | `/api/documents` | 文档列表 |
| GET | `/api/documents/{id}` | 文档详情/状态 |
| DELETE | `/api/documents/{id}` | 删除文档 |
| POST | `/api/chat/sessions` | 新建会话 |
| GET | `/api/chat/sessions` | 会话列表 |
| POST | `/api/chat/ask` | 非流式问答 |
| POST | `/api/chat/stream` | **流式问答（SSE）** |
| GET | `/api/chat/sessions/{id}/messages` | 会话历史 |
| GET | `/health` | 健康检查 |

除 `/auth/register`、`/auth/login`、`/health` 外，均需在请求头携带 Token：

```
Authorization: Bearer <你的token>
```

---

## 八、运行测试

后端内置 20 个单元测试，**离线可跑**（使用模拟嵌入与模拟大模型，无需 API Key）：

```powershell
cd backend
.venv\Scripts\activate
pytest -q
```

预期输出：`20 passed`。

---

## 九、常见问题排查（FAQ）

### Q1：`docker compose up` 卡在镜像拉取或很慢
- 国内网络可配置镜像加速器（Docker Desktop → Settings → Docker Engine，添加国内镜像源）。
- 首次构建需下载 MySQL、Python 等基础镜像，属正常，耐心等待。

### Q2：容器一直重启 / `mysql` 不健康
- 首次启动 MySQL 初始化需要时间，等 30–60 秒再看 `docker compose ps`。
- 若一直重启，`docker compose logs mysql` 查看报错。

### Q3：上传文档后一直是 `PENDING`，不变成 `COMPLETED`
- 检查 worker 容器是否在运行：`docker compose ps` 看 `rag-worker` 是否为 `Up`。
- `docker compose logs worker` 看是否报错（通常是 API Key 没配或网络问题）。

### Q4：问答时提示「资料中未找到相关信息」
- 说明该内容没被检索到。先确认文档已 `COMPLETED`；
- 换更精准的关键词提问；或检查文档是否包含该内容。

### Q5：报错 `401` 或「登录已过期」
- Token 24 小时过期，重新登录即可。

### Q6：报错 `InvalidApiKey` / 权限相关
- 确认 `.env` 的 `DASHSCOPE_API_KEY` 正确；
- 确认已开通 `qwen-plus` / `text-embedding-v3` / `gte-rerank` 三个模型权限。

### Q7：端口被占用（8000 / 8501 / 3306）
- 修改 `docker-compose.yml` 中对应端口映射（如 `8000:8000` → `8009:8000`）。

### Q8：如何排查问题更高效？
- 后端日志：`docker compose logs -f api`
- Worker 日志：`docker compose logs -f worker`
- 健康检查：浏览器访问 `http://localhost:8000/health`，应返回 `{"code":0,...}`。

---

## 附：项目目录速览

```
E:\实习\AI系统\
├── .env.example          # 环境变量模板（复制为 .env）
├── docker-compose.yml    # 一键部署配置
├── README.md             # 项目总览
├── 使用与环境配置说明书.md  # 本文件
├── 面试讲解与自我介绍.md    # 面试准备
├── backend/              # 后端代码 + 测试
│   ├── app/              # 应用代码
│   └── tests/            # pytest 测试
├── frontend/             # Streamlit 前端
└── data/                 # 运行数据（FAISS 索引、上传文件）
```